syntax="proto3";

package grpc_remote_control;

service RemoteControlService {
  // Sends a greeting
  rpc CheckStatus (RequestStatus) returns (ResponseStatus) {}
}

message RequestStatus {
}

message ResponseStatus {
  bool alive = 1;
}
